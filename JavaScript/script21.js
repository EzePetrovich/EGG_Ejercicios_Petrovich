let person = {
    name : 'J Ezequiel',
    surname : 'Petrovich',
    old : 23,
    sex : 'Hombre',
    weight : 68,
    height : 174
};

let valsPerson = Object.values(person);

console.log(valsPerson);

/*

let strPerson = JSON.stringify(person); // Convierte un objeto a JSON

console.log(strPerson);

let objPerson = JSON.parse(strPerson); // Convierte un JSON a objeto

console.log(objPerson);

 */